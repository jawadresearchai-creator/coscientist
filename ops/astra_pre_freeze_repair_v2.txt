Launch authorization for the outcome-blind Astra pre-freeze V2 design repair.

Scope is limited to pre-event data, EDGAR current-report metadata, empirical pre-event placebo power, immutable design V2 publication, and paper-local pre-freeze state repair. Astra event-window outcomes remain locked.

Relaunch: hardened SEC daily-index transport requests identity encoding and validates the master-index header/row count before parsing.

Relaunch 2: validator now accepts SEC's actual stable header prefix `CIK|Company Name|Form Type|Date Filed|` (including the observed `File Name` final column) after the prior fail-closed mismatch. Proceed with corrected EDGAR join, placebo-power calculation, exposure promotion rule, immutable V2 publication, and paper-local pre-freeze repair only.

Relaunch 3: SEC master-index `Date Filed` is now normalized from compact `YYYYMMDD` to ISO `YYYY-MM-DD` at the parser boundary. Regression tests pass. Proceed with the same outcome-blind V2 repair scope; do not inspect Astra event-window outcomes.

Relaunch 4: baseline empirical common-date power failed the unchanged 0.50pp MDE80 threshold. Run the predeclared outcome-blind rescue tournament only: rolling exact-200 pseudo-event models; fixed SPY/MULTI3, OLS/WLS and volatility-screen candidate order; first 25 placebo windows for selection; later 25 for one-shot validation. Publish V2 and patch paper-local state only if the calibration-selected candidate passes untouched validation. Do not inspect Astra event-window outcomes.

Relaunch 5: the rescue is now routed through the hardened SEC identity-encoding/header-validating transport. All compile/test/integrity gates pass. Execute the same frozen rescue tournament without changing candidate order or threshold.

Relaunch 6: zero/non-finite residual sigma histories are conservatively assigned the p90 sigma before inverse-variance weighting, giving them the lowest allowed precision weight rather than infinite precision. This rule is unit-tested. Candidate order and the 0.50pp threshold remain unchanged.

Stage2 launch: execute ops/astra_stage2_power_rescue_spec.json exactly. Use only eligible_event_dates[-200:-100], with the first 25 two-day windows for calibration and the later 25 for untouched validation. Rich controls are frozen to log market cap, ROA, selected-model residual sigma, beta200, alpha200, hierarchical SIC4/SIC3/SIC2 fixed effects, exchange fixed effects, and pre-event filing-form fixed effects. Candidate order is frozen in the spec. Require calibration MDE80 <=0.45pp and untouched validation MDE80 <=0.50pp. Publish nothing and mutate no paper state on failure. Astra event-window outcomes remain locked.
